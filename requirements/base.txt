# Runtime dependencies shared by every environment.
#
# Install with:  pip install -r requirements/base.txt
# For local development use requirements/dev.txt, which includes this file.

# Django and Django REST Framework
Django>=5.2,<5.3
djangorestframework>=3.18.0,<4.0
django-filter>=25.1

# Database
psycopg[binary]>=3.3.5
# Managed hosts hand you a single DATABASE_URL rather than separate parts.
dj-database-url>=2.3

# Configuration
python-dotenv>=1.1

# API schema and documentation
drf-spectacular>=0.28

# Bearer tokens for clients with no cookie jar -- the mobile app. Added to
# the authentication classes rather than substituted for them, so the browser
# keeps using the session cookie. See apps/authentication/authentication_token.py.
djangorestframework-simplejwt>=5.5

# CORS handling
django-cors-headers>=4.7

# Asynchronous task processing
celery>=5.5,<6.0
redis>=5.2

# Caching
django-redis>=7.0.0

# Email (AWS SES)
boto3>=1.43.76
django-ses>=4.4

# Third-party login
social-auth-app-django>=5.4

# Billing (only imported when STRIPE_ENABLED is true; safe to remove along
# with apps/subscriptions if the project does not need billing)
stripe>=15.5.1

# Retries for outbound API calls
tenacity>=9.1.4

# Error tracking. Imported only when SENTRY_DSN is set, but kept in base
# rather than prod so the code path is importable in the test suite.
sentry-sdk[django]>=2.20

# TOTP for two-factor authentication (only imported when
# TWO_FACTOR_ENABLED is true; safe to remove with apps/authentication's
# two_factor module if the project does not need it)
pyotp>=2.9

# Model Context Protocol. Only imported when MCP_SERVER_ENABLED is true;
# safe to remove along with apps/mcp_server if the application does not need
# to be callable by agents.
mcp>=2.0
# The MCP connector: Anthropic fetches a remote MCP server on our behalf.
# Only needed with MCP_CLIENT_ENABLED, and only for the connector transport.
anthropic>=1.0

# S3 storage for uploads (only used when AWS_STORAGE_BUCKET_NAME is set)
django-storages[s3]>=1.14
