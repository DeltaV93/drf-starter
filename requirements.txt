# Requirements are split by environment under requirements/.
#
#   requirements/base.txt  runtime dependencies shared everywhere
#   requirements/dev.txt   base + testing, linting, debugging
#   requirements/prod.txt  base + gunicorn and whitenoise
#
# This file installs the development set so that a bare
# `pip install -r requirements.txt` gives you a working local environment.

-r requirements/dev.txt
